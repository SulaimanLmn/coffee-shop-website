# Database (matches docker-compose.yml credentials)
DATABASE_URL="postgresql://coffee:coffee@localhost:5432/coffee_shop?schema=public"

# Stripe
STRIPE_SECRET_KEY="sk_test_xxx"
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_xxx"
STRIPE_WEBHOOK_SECRET="whsec_xxx"

# Public base URL of the app (used to build Stripe redirect URLs safely).
# Set this in production to https://your-domain.com. Leave as-is for local dev.
APP_URL="http://localhost:3000"

# Admin (hardcoded password for the MVP admin dashboard)
ADMIN_PASSWORD="admin123"
